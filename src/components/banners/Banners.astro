<style>
    .banner {
        width: 100%;
        height: 100vh; /* 100% de la altura de la pantalla */
        background-image: url('/img/42520.jpeg'); /* Ruta relativa a la imagen */
        background-size: cover; /* Para que la imagen cubra todo el espacio */
        background-position: center; /* Centrar la imagen */
        position: relative; /* Para poder posicionar el overlay */
    }

    .overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.6); /* Color negro con opacidad del 60% */
        display: flex;
        flex-direction: column; /* Apilar elementos verticalmente */
        justify-content: center; /* Centrar horizontalmente */
        align-items: center; /* Centrar verticalmente */
    }
    
    .description {
        color: white; /* Color del texto */
        font-size: 1.5rem; /* Tamaño de fuente relativo */
        margin: 2rem; /* Margen relativo */
        padding: 0 1.25rem; /* Padding a los lados */
        padding-left: 1.5rem; /* Espacio adicional a la izquierda para la línea */
        border-left: 5px solid #ff5100; /* Línea azul a la izquierda */
        text-align: justify; /* Justificar el texto */
        font-weight: 300;
    }

    /* Estilos específicos para dispositivos móviles */
    @media (max-width: 768px) {
        .description {
            font-size: 15px; /* Tamaño de fuente más pequeño */
            margin: 1rem; /* Reducir margen */
            padding-left: 1.25rem; /* Reducir padding izquierdo */
            
        }
    }

    /* Estilos específicos para tablets */
    @media (min-width: 769px) and (max-width: 1024px) {
        .description {
            font-size: 20px; /* Tamaño de fuente intermedio */
            margin: 1.5rem; /* Ajustar margen */
            padding-left: 1.5rem; /* Ajustar padding izquierdo */
            margin-top: 15em;

        }
    }

    /* Estilos específicos para pantallas grandes */
    @media (min-width: 1025px) {
        .description {
            font-size: 20px; /* Tamaño de fuente más grande */
            margin-left: 5em; 
            margin-top: 15em;
            margin-right: 5em;
            padding-left: 2rem; /* Espacio adicional a la izquierda para la línea */
        }
    }
</style>


<div class="banner">
    <div class="overlay">
        <div class="description">En Proditel, nos dedicamos a ofrecer soluciones integrales para satisfacer 
                                                las necesidades tecnológicas y de comunicación de nuestros clientes. Nuestro compromiso es brindar servicios
                                                de alta calidad que impulsan el crecimiento y la eficiencia de las empresas en diversos sectores.
        </div>
    </div>
</div>
