<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ejemplo de Navegación</title>
    <style>
        html {
            scroll-behavior: smooth;
        }

        .underline-animation::after {
            content: '';
            display: block;
            width: 0;
            height: 2px;
            background-color: #144478;
            transition: width 0.3s;
        }

        .underline-animation:hover::after {
            width: 100%;
        }

        .underline-animation {
            cursor: pointer;
        }

        .header-container {
            display: flex;
            flex-direction: column;
        }

        .mini-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            background-color: #ff5000; /* Color naranja */
            color: white;
            text-align: center;
            padding: 8px 0;
            font-size: 12px;
            font-weight: bold;
        }

        .mini-header-left {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 40%;
        }

        .mini-header-right {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 10%;
            margin-right: 30px; /* Ajustar según sea necesario */
        }

        .mini-header-icons {
            display: flex;
            gap: 10px;
        }

        .mini-header-icon {
            font-size: 12px;
        }

        .navbar {
            width: 100%;
            background-color: rgb(255, 255, 255); /* Fondo gris opaco */
            position: absolute;
            z-index: 50;
            color: #144478;
        }

        .navbar-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 16px 32px;
        }

        .navbar-links {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-left: auto; /* Mueve las rutas al lado derecho */
        }

        .navbar-link {
            font-weight: bold;
            font-size: 10px;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-right: 30px; /* Espacio entre rutas */
        }

        .navbar-link:first-child {
            margin-left: 1em; /* Eliminar espacio extra en la primera ruta */
        }

        .contact-button {
            background-color: #ff5000;
            color: white;
            padding: 8px 16px;
            border-radius: 9999px;
            text-transform: uppercase;
            font-size: 10px;
            letter-spacing: 3px;
            cursor: pointer;
            transition: background-color 0.3s;
            font-weight: bold;
        }

        .contact-button:hover {
            background-color: #e64500; /* Slightly darker shade for hover */
        }

        .logo {
            width: 190px; /* Tamaño reducido */
            margin-left: 100px; /* Espacio a la derecha del logo */
        }
    </style>
</head>
<body>
    <div class="header-container">
        <div class="navbar">
            <div class="mini-header">
                <div class="mini-header-left">
                    <div class="mini-header-icons">
                        <i class="fas fa-map-marker-alt mini-header-icon"></i>
                        <span>Ubicación example</span>
                        <i class="fas fa-envelope mini-header-icon"></i>
                        <span>Correo example</span>
                        <i class="fas fa-phone-alt mini-header-icon"></i>
                        <span>Teléfono example</span>
                    </div>
                </div>
                <div class="mini-header-right">
                    <div class="mini-header-icons">
                        <i class="fab fa-facebook-f mini-header-icon"></i>
                        <i class="fab fa-instagram mini-header-icon"></i>
                        <i class="fab fa-twitter mini-header-icon"></i>
                        <i class="fab fa-linkedin-in mini-header-icon"></i>
                    </div>
                </div>
            </div>

            <div class="navbar-content">
                <a href="/">
                    <img src="/img/logo.svg" alt="Logo" class="logo">
                </a>
                <div class="navbar-links">
                    <div class="navbar-link underline-animation">
                        <a href="#nosotros">
                            <h3>Nosotros</h3>
                        </a>
                    </div>
                    <div class="navbar-link underline-animation">
                        <a href="#servicios">
                            <h3>Servicios</h3>
                        </a>
                    </div>
                    <div class="navbar-link underline-animation">
                        <a href="#proyectos">
                            <h3>Proyectos</h3>
                        </a>
                    </div>
                </div>
                <button class="contact-button">Contáctanos</button>
            </div>
        </div>
    </div>


    <script>
        // No se necesita JavaScript adicional ya que el desplazamiento suave se maneja con CSS
    </script>
</body>
</html>
